---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: instance
    image: ubuntu:19.10
provisioner:
  name: ansible
  # log: true
  lint:
    name: ansible-lint
verifier:
  name: testinfra
  # options:
  #   s: True
  lint:
    name: flake8
